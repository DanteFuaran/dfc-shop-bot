# 🚨 СРОЧНЫЕ МЕРЫ БЕЗОПАСНОСТИ

## ⚠️ Обнаружена утечка конфиденциальных данных

Файл `.env` с чувствительными данными был обнаружен в публичном репозитории GitHub. Файл был удалён из истории Git, но необходимо предпринять дополнительные меры безопасности.

## 📋 ОБЯЗАТЕЛЬНЫЕ ДЕЙСТВИЯ

### 1. Смена токена Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите ваш бот
4. Нажмите **"API Token"**
5. Нажмите **"Revoke current token"** (Отозвать текущий токен)
6. Скопируйте новый токен
7. Обновите файл `.env`:
   ```bash
   nano /opt/dfc-shop-bot/.env
   ```
8. Замените значение `BOT_TOKEN=` на новый токен
9. Перезапустите бота:
   ```bash
   cd /opt/dfc-shop-bot && docker compose restart
   ```

### 2. Смена API токена Remnawave

1. Войдите в панель Remnawave
2. Перейдите в **Settings → API Tokens**
3. Удалите старый токен
4. Создайте новый токен
5. Скопируйте новый токен
6. Обновите файл `.env`:
   ```bash
   nano /opt/dfc-shop-bot/.env
   ```
7. Замените значение `REMNAWAVE_TOKEN=` на новый токен
8. Перезапустите бота:
   ```bash
   cd /opt/dfc-shop-bot && docker compose restart
   ```

### 3. Генерация новых ключей шифрования и секретов

```bash
cd /opt/dfc-shop-bot

# Создайте резервную копию текущего .env
cp .env .env.backup

# Сгенерируйте новые ключи
NEW_CRYPT_KEY=$(openssl rand -base64 32 | tr -d '\n')
NEW_BOT_SECRET=$(openssl rand -hex 64 | tr -d '\n')
NEW_DB_PASSWORD=$(openssl rand -hex 32 | tr -d '\n')
NEW_REDIS_PASSWORD=$(openssl rand -hex 32 | tr -d '\n')

# Обновите .env файл
sed -i "s|^APP_CRYPT_KEY=.*|APP_CRYPT_KEY=${NEW_CRYPT_KEY}|" .env
sed -i "s|^BOT_SECRET_TOKEN=.*|BOT_SECRET_TOKEN=${NEW_BOT_SECRET}|" .env
sed -i "s|^DATABASE_PASSWORD=.*|DATABASE_PASSWORD=${NEW_DB_PASSWORD}|" .env
sed -i "s|^REDIS_PASSWORD=.*|REDIS_PASSWORD=${NEW_REDIS_PASSWORD}|" .env

# Также обновите пароль PostgreSQL
sed -i "s|^POSTGRES_PASSWORD=.*|POSTGRES_PASSWORD=${NEW_DB_PASSWORD}|" .env
```

### 4. Пересоздание базы данных и Redis с новыми паролями

```bash
cd /opt/dfc-shop-bot

# ВНИМАНИЕ: Это удалит все данные! Создайте резервную копию!
# Создание резервной копии БД
docker compose exec remnashop-db pg_dump -U remnashop remnashop > /opt/backups/db_backup_$(date +%Y%m%d_%H%M%S).sql

# Остановите все контейнеры
docker compose down

# Удалите volumes с данными
docker volume rm dfc-shop-bot_postgres_data dfc-shop-bot_redis_data

# Запустите заново с новыми паролями
docker compose up -d

# Восстановите данные из резервной копии (если нужно)
# docker compose exec -T remnashop-db psql -U remnashop remnashop < /opt/backups/db_backup_XXXXXX.sql
```

### 5. Проверка активных сессий и подключений

После смены всех учётных данных:

1. Проверьте логи на наличие подозрительной активности:
   ```bash
   docker compose logs remnashop | grep -i "error\|unauthorized\|failed"
   ```

2. Убедитесь, что бот работает корректно:
   ```bash
   docker compose ps
   docker compose logs remnashop --tail=50
   ```

### 6. Мониторинг безопасности

В течение следующих 24-48 часов:

- ✅ Отслеживайте логи на предмет необычной активности
- ✅ Проверьте, нет ли несанкционированных изменений в панели Remnawave
- ✅ Убедитесь, что нет незнакомых пользователей в системе
- ✅ Проверьте транзакции и финансовую активность

## 🔒 Рекомендации на будущее

### Дополнительные меры безопасности:

1. **Включите двухфакторную аутентификацию (2FA):**
   - На GitHub аккаунте
   - В панели Remnawave (если доступно)
   - В Telegram

2. **Настройте мониторинг:**
   - Подпишитесь на уведомления GitHub о push в репозиторий
   - Настройте алерты для критических событий в боте

3. **Регулярно обновляйте пароли:**
   - Меняйте API токены каждые 3-6 месяцев
   - Используйте уникальные сложные пароли

4. **Проверьте .gitignore:**
   Убедитесь, что следующие файлы исключены из Git:
   ```gitignore
   .env
   .env.local
   .env.*.local
   *.log
   backups/
   logs/
   ```

5. **Настройте автоматические резервные копии:**
   ```bash
   # Добавьте в crontab
   crontab -e
   
   # Резервное копирование каждый день в 3:00
   0 3 * * * cd /opt/dfc-shop-bot && docker compose exec -T remnashop-db pg_dump -U remnashop remnashop > /opt/backups/db_backup_$(date +\%Y\%m\%d).sql
   ```

## 📞 Контакты поддержки

Если у вас возникли вопросы или подозрительная активность:

1. **GitHub Issues:** https://github.com/DanteFuaran/dfc-shop-bot/issues
2. **Telegram Support:** @your_support_username (указать ваш)

## ✅ Чек-лист выполненных действий

- [ ] Токен Telegram бота изменён
- [ ] API токен Remnawave изменён
- [ ] Ключи шифрования обновлены
- [ ] Пароли БД и Redis изменены
- [ ] Контейнеры перезапущены с новыми данными
- [ ] Резервная копия данных создана
- [ ] Логи проверены на подозрительную активность
- [ ] .gitignore обновлён
- [ ] 2FA включен на всех сервисах
- [ ] Настроен мониторинг

---

**Дата инцидента:** 28 января 2026
**Статус:** Файл .env удалён из репозитория и истории Git
**Требуется:** Смена всех учётных данных
