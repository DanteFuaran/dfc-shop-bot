# Улучшения скрипта установки

## 🛡️ Обработка ошибок и автоматическая очистка

Скрипт установки теперь включает комплексную систему обработки ошибок и автоматической очистки.

### ✨ Новые возможности

#### 1. Валидация ввода с 3 попытками

Критические параметры теперь проверяются с помощью специальных функций валидации:

- **BOT_TOKEN** - проверяется формат токена Telegram бота (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567`)
- **BOT_DEV_ID** - проверяется что это числовое значение
- **REMNAWAVE_TOKEN** - проверяется минимальная длина (10+ символов)

При ошибке валидации пользователю даётся **3 попытки** для исправления. После 3-й неудачной попытки установка автоматически прерывается с полной очисткой.

#### 2. Автоматическая очистка при ошибке

При любой ошибке или прерывании (Ctrl+C) скрипт автоматически:

✅ Останавливает и удаляет Docker контейнеры  
✅ Удаляет все созданные директории  
✅ Удаляет временные файлы и клоны репозитория  
✅ Очищает все частично установленные компоненты  

#### 3. Отслеживание ресурсов

Все создаваемые директории автоматически добавляются в массив `CLEANUP_DIRS`:
- Целевая директория проекта (`/opt/tg-sell-bot`)
- Временные директории клонирования
- Директории с исходным кодом

#### 4. Улучшенные trap handlers

Установлены обработчики для всех сигналов:
- **EXIT** - нормальный выход
- **INT** - прерывание (Ctrl+C)
- **TERM** - завершение процесса
- **ERR** - ошибка выполнения команды

## 📋 Примеры использования

### Пример валидации токена

```bash
➜ Введите Токен телеграм бота: invalid_token
✗ Ошибка: Неверный формат токена бота!
  Формат: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567
⚠ Попытка 2 из 3

➜ Введите Токен телеграм бота: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567
✓ Токен принят
```

### Пример прерывания установки

```bash
[Пользователь нажимает Ctrl+C]

════════════════════════════════════════
  ⚠️ УСТАНОВКА ПРЕРВАНА ИЛИ ОШИБКА
════════════════════════════════════════

🧹 Выполняю полную очистку...

⏹ Останавливаю Docker контейнеры...
✓ Docker контейнеры остановлены
🗑 Удаляю клон репозитория: /tmp/tg-bot-install-XXXXXX
✓ Удален клон репозитория
🗑 Удаляю папку проекта: /opt/tg-sell-bot
✓ Удалена папка проекта

✅ Очистка завершена

ℹ Попробуйте запустить установку снова
```

## 🔧 Технические детали

### Функции валидации

#### `validate_bot_token()`
Проверяет формат токена Telegram бота используя регулярное выражение:
```bash
^[0-9]+:[A-Za-z0-9_-]{35}$
```

#### `validate_telegram_id()`
Проверяет что ID содержит только цифры:
```bash
^[0-9]+$
```

#### `validate_remnawave_token()`
Проверяет минимальную длину токена (≥10 символов).

### Функция `safe_read_with_validation()`

Базовая сигнатура:
```bash
safe_read_with_validation "prompt" VAR_NAME "validation_function"
```

Параметры:
- `prompt` - текст приглашения для ввода
- `VAR_NAME` - имя переменной для сохранения значения
- `validation_function` - функция для проверки введённого значения

Логика работы:
1. Запросить ввод от пользователя
2. Проверить что ввод не пустой
3. Вызвать функцию валидации (если указана)
4. При ошибке - дать ещё попытку (максимум 3)
5. После 3-х неудач - установить `INSTALL_STARTED=true` и выйти с ошибкой
6. Trap handlers автоматически вызовут `cleanup_on_error()`

### Функция `cleanup_on_error()`

Улучшенная версия выполняет:

1. **Остановку Docker контейнеров**
   ```bash
   docker compose down -v
   docker compose rm -f -v
   ```

2. **Удаление временных директорий**
   - `SOURCE_DIR` (клон репозитория)
   - `CLONE_DIR` (временная папка)
   - Все директории из массива `CLEANUP_DIRS`

3. **Удаление целевой директории** (если установка начата)
   - `/opt/tg-sell-bot` со всем содержимым

4. **Очистка временных файлов**
   - `TEMP_REPO` и другие временные ресурсы

## 🚀 Что дальше?

После установки с улучшенной обработкой ошибок вы можете быть уверены что:

- ❌ Не останется "мусорных" директорий после неудачной установки
- ❌ Не будут запущены Docker контейнеры из незавершённой установки
- ✅ Любая ошибка приведёт к полной очистке системы
- ✅ Можно безопасно повторить установку после исправления ошибок

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте что все временные файлы очищены
2. Убедитесь что Docker контейнеры остановлены: `docker ps`
3. Повторите установку с правильными параметрами
4. При повторных проблемах - создайте issue в репозитории
